info:
  name: Health Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/health-status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
