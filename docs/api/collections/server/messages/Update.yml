info:
  name: Update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{api_url}}/messages/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "message": "Hello World!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
