info:
  name: List messages
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/chats/:id/messages"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
