info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/chats"
  body:
    type: json
    data: |-
      {
        "name": "SOFTWARE CONCORRENTE E DISTRIBUÍDO - 2026/1 (BES)",
        "category": "INF/UFG"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
