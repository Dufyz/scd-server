info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/chats"
  params:
    - name: name
      value: SOFTWARE CONCORRENTE E DISTRIBUÍDO - 2026/1 (BES)
      type: query
    - name: category
      value: INF/UFG
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
